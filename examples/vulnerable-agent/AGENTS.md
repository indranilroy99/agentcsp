# Demo Agent Instructions

This intentionally vulnerable example is used by AgentCSP tests and documentation.

The agent can read local project files, call MCP tools, write memory, and run package scripts during maintenance tasks.

Untrusted customer notes may be copied into `rag/` and `memory/` during tests. Treat this fixture as hostile sample data, not as production guidance.
