{
  "name": "@agentcsp/workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source control plane for discovering, testing, and enforcing context security policy across AI agents, tools, MCP servers, RAG, memory, and runtime actions.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/indranilroy99/agentcsp.git"
  },
  "bugs": {
    "url": "https://github.com/indranilroy99/agentcsp/issues"
  },
  "homepage": "https://github.com/indranilroy99/agentcsp#readme",
  "keywords": [
    "ai-security",
    "agent-security",
    "context-security-policy",
    "mcp",
    "sarif",
    "security-scanner"
  ],
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "packageManager": "pnpm@11.0.9",
  "scripts": {
    "agentcsp": "node packages/cli/dist/index.js",
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "generate:schemas": "jiti scripts/generate-schemas.ts",
    "lint": "tsc -p packages/core/tsconfig.json --noEmit && tsc -p packages/cli/tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "pnpm verify:schemas && pnpm verify:versions && pnpm verify:rules && pnpm build && pnpm verify:packages && pnpm verify:ci-examples && pnpm lint && pnpm test && pnpm verify:fixtures && pnpm audit --audit-level moderate",
    "verify:fixtures": "pnpm build && pnpm agentcsp scan examples/vulnerable-agent --out .agentcsp --format json,md,sarif --include-logs --quiet && pnpm agentcsp scan examples/safe-agent --out .agentcsp-safe --format json,md --quiet && node scripts/verify-fixture-outputs.mjs && node scripts/verify-policy-artifact-redaction.mjs && node scripts/verify-rule-artifact-redaction.mjs",
    "verify:ci-examples": "node scripts/verify-ci-examples.mjs",
    "verify:packages": "node scripts/verify-package-artifacts.mjs",
    "verify:rules": "jiti scripts/verify-rules.ts",
    "verify:schemas": "jiti scripts/verify-schemas.ts",
    "verify:versions": "node scripts/verify-versions.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.13.2",
    "esbuild": "^0.28.1",
    "jiti": "^2.7.0",
    "typescript": "^5.8.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.9",
    "yaml": "^2.9.0",
    "zod-to-json-schema": "^3.24.5"
  },
  "pnpm": {
    "overrides": {
      "esbuild": "^0.28.1"
    },
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
