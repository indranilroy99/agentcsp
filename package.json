{
  "name": "@agentcsp/workspace",
  "version": "0.2.0",
  "private": true,
  "description": "Local-first static security analysis for AI agent repositories, MCP servers, tools, instructions, memory, and CI automation.",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/indranilroy99/agentcsp.git"
  },
  "bugs": {
    "url": "https://github.com/indranilroy99/agentcsp/issues"
  },
  "homepage": "https://github.com/indranilroy99/agentcsp#readme",
  "keywords": [
    "ai-security",
    "agent-security",
    "context-security-policy",
    "mcp",
    "sarif",
    "security-scanner"
  ],
  "engines": {
    "node": ">=22"
  },
  "type": "module",
  "packageManager": "pnpm@11.0.9",
  "scripts": {
    "agentcsp": "node packages/cli/dist/index.js",
    "benchmark:rules": "jiti scripts/benchmark-recommended.ts",
    "benchmark:scale": "jiti scripts/benchmark-scan-scale.ts",
    "build": "pnpm -r build",
    "clean": "pnpm -r clean",
    "generate:schemas": "jiti scripts/generate-schemas.ts",
    "lint": "tsc -p packages/core/tsconfig.json --noEmit && tsc -p packages/cli/tsconfig.json --noEmit",
    "sbom": "node scripts/generate-sbom.mjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "pnpm verify:schemas && pnpm verify:versions && pnpm verify:docs && pnpm verify:rules && pnpm benchmark:rules && pnpm build && pnpm verify:packages && pnpm verify:ci-examples && pnpm verify:sbom && pnpm lint && pnpm test && pnpm verify:fixtures && pnpm audit --audit-level moderate",
    "verify:release": "pnpm verify && pnpm benchmark:scale",
    "verify:fixtures": "pnpm build && pnpm agentcsp scan examples/vulnerable-agent --out .agentcsp --ruleset extended --artifact-profile internal --format json,md,sarif --include-logs --quiet && pnpm agentcsp scan examples/safe-agent --out .agentcsp-safe --ruleset extended --artifact-profile internal --format json,md --quiet && node scripts/verify-fixture-outputs.mjs && node scripts/verify-policy-artifact-redaction.mjs && node scripts/verify-rule-artifact-redaction.mjs",
    "verify:ci-examples": "node scripts/verify-ci-examples.mjs",
    "verify:docs": "node scripts/verify-docs.mjs",
    "verify:packages": "node scripts/verify-package-artifacts.mjs",
    "verify:rules": "jiti scripts/verify-rules.ts",
    "verify:sbom": "node scripts/verify-sbom.mjs",
    "verify:schemas": "jiti scripts/verify-schemas.ts",
    "verify:versions": "node scripts/verify-versions.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "esbuild": "^0.28.1",
    "jiti": "^2.7.0",
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10",
    "yaml": "^2.9.0",
    "zod-to-json-schema": "^3.24.5"
  },
  "pnpm": {
    "overrides": {
      "esbuild": "^0.28.1"
    },
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
