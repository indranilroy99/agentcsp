name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  verify:
    name: Verify
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.0.9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: TypeScript check
        run: pnpm lint

      - name: Verify exported schemas
        run: pnpm verify:schemas

      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: Verify package artifacts
        run: pnpm verify:packages

      - name: Audit high and critical vulnerabilities
        run: pnpm audit --audit-level high

      - name: AgentCSP scan vulnerable fixture
        run: pnpm agentcsp scan examples/vulnerable-agent --out .agentcsp --format json,md,sarif --quiet

      - name: AgentCSP scan safe fixture
        run: pnpm agentcsp scan examples/safe-agent --out .agentcsp-safe --format json,md --quiet

      - name: Verify fixture outputs
        run: pnpm verify:fixtures
